# Contributing

欢迎提交 PR 或 Issue：

- **规范本身**：修改 `SKILL.md` 时保持与 `examples.md` / `scripts/render_gallery.R` 一致；更新 `CHANGELOG.md`。
- **示例脚本**：`scripts/render_gallery.R` 应保持仅依赖 CRAN 上的 `ggplot2` 与 `patchwork`，避免引入本仓库其他路径。

Fork 后若只取本 skill，可只复制 `scientific-figure-spec/` 目录。
